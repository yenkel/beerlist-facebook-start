app.factory('authFactory', function($http) {
  var auth = {};

  auth.currentUser = {};

  auth.logout = function(user) {
    //todo
  };

  auth.login = function(user) {
    //todo
  };

  return auth;
});
